"use client"

import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog"

const Dialog = AlertDialog
const DialogTrigger = AlertDialogTrigger
const DialogContent = AlertDialogContent
const DialogHeader = AlertDialogHeader
const DialogTitle = AlertDialogTitle
const DialogDescription = AlertDialogDescription
const DialogFooter = AlertDialogFooter
const AlertDialogCancel = AlertDialogCancel
const AlertDialogAction = AlertDialogAction

export {
  Dialog,
  DialogTrigger,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogFooter,
  AlertDialogCancel as DialogCancel,
  AlertDialogAction as DialogAction,
}

